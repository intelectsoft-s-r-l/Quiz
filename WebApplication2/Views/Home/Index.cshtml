@using WebApplication2.Models.API;
@model IEnumerable<Questionnaire>

@{
    ViewBag.Title = "Questionnaire";
    ViewBag.pTitle = "Questionnaire";
    ViewBag.pageTitle = "Questionnaires";
    Layout = "~/Views/_Shared/_Layout.cshtml";
}

<script src="~/js/app.js"></script>

<div class="row mb-2">
    <div class="col-md-6">
        <div class="mb-3">
            <a type="button" class="btn btn-success waves-effect waves-light" href="@Url.Action("Create", "Home")">Add Questionnaire</a>
        </div>
    </div>

    <div class="col-md-6">
        <div class="form-inline float-md-end mb-3">
            <div class="search-box ml-2">
                <div class="position-relative">
                    <input type="text" class="form-control rounded border-0" placeholder="Search...">
                    <i class="mdi mdi-magnify search-icon"></i>
                </div>
            </div>

        </div>
    </div>


</div>
<!-- end row -->

<div class="row">
    @foreach(var item in Model)
    {
    <div class="col-xl-4 col-sm-6">
        <div class="card">
            <div class="card-body">
                <div class="media">
@*                     <div class="avatar-sm me-4">
                        <span class="avatar-title bg-soft-primary text-primary font-size-16 rounded-circle">
                            M
                        </span>
                    </div> *@
                    <div class="media-body align-self-center">
                        <div class="border-bottom pb-1">
                            <h5 class="text-truncate font-size-16 mb-1"><a href="#" class="text-dark">@item.name</a></h5>
                            <p class="text-muted">
                                <i class="mdi mdi-account me-1"></i> @item.company
                            </p>
                        </div>
                        <div class="row">
@*                             <div class="col-6">
                                <div class="mt-3">
                                    <p class="text-muted mb-2">Questions</p>
                                        <h5 class="font-size-16 mb-0">@*item.questions*</h5>
                                </div>
                            </div> *@
                            <div class="col-12">
                                <div class="mt-3">
                                    <p class="text-muted mb-2">Other</p>
                                        <a href="@Url.Action("Index", "Detail")" style="width: 100%;" type="button" class="btn btn-secondary waves-effect waves-light">Detail</a>
                                        <button type="button" style="width: 100%; margin-top: 10px;" class="btn btn-danger waves-effect waves-light" data-bs-toggle="modal" data-bs-target=".bs-example-modal-lg" onclick="drawPatrialView('/Home/Delete/'+@item.oid, 'lgModalBody')">Delete</button>
                                        <!-- sample modal content -->
                                        
                                        
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
    }
</div>
